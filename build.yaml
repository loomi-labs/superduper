targets:
  $default:
    builders:
      # Nested model lists (BikeState.customModes) round-trip through json only
      # when toJson is called on the elements instead of casting them.
      json_serializable:
        options:
          explicit_to_json: true
